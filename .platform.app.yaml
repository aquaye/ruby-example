name: "ruby_example"
type: "ruby:2.5"
disk: 1024

hooks:
  build: |
    bundle install
    rake assets:precompile
  deploy: |
    rails db:migrate

web:
  commands:
    start: 'rails server -p $PORT'
  locations:
    '/':
      root: 'public'
      passthru: true
      allow: true